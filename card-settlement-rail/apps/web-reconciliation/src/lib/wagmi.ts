import {createConfig,http} from "wagmi"; import {baseSepolia,arbitrumSepolia} from "wagmi/chains"; import {injected,walletConnect} from "wagmi/connectors"; const projectId=process.env.NEXT_PUBLIC_WC_PROJECT_ID; export const wagmiConfig=createConfig({chains:[baseSepolia,arbitrumSepolia],connectors:[injected(),...(projectId?[walletConnect({projectId})]:[])],transports:{[baseSepolia.id]:http(),[arbitrumSepolia.id]:http()}})