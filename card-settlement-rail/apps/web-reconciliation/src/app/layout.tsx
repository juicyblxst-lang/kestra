import "@card-settlement/ui/styles.css"; import type {Metadata} from "next"; import {WalletProvider} from "../components/wagmi-provider"; export const metadata:Metadata={title:"Reconciliation Console",description:"Card Settlement Rail operational console"}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body className="min-h-screen bg-slate-950 text-slate-100"><WalletProvider>{children}</WalletProvider></body></html>}