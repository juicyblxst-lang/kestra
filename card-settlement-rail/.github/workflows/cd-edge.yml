name: CD Edge
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  deploy-edge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Round 1: edge deployment intentionally disabled."
