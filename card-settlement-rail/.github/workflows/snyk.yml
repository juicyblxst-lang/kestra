name: Snyk
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  snyk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Round 1: Snyk execution intentionally disabled."
