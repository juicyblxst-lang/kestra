name: Reconciliation Cron
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  reconciliation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Round 1: reconciliation scheduling intentionally disabled."
