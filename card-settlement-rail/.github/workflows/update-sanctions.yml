name: Update sanctions data

on:
  workflow_dispatch:

jobs:
  update-sanctions:
    runs-on: ubuntu-latest
    if: ${{ false }}
    steps:
      - uses: actions/checkout@v4
      - run: echo "Live sanctions ingestion is intentionally disabled in Round 1."
