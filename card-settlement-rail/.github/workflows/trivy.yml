name: Trivy
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  trivy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Round 1: Trivy execution intentionally disabled."
