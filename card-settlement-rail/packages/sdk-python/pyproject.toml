[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"
[project]
name = "card-settlement-sdk"
version = "0.1.0"
description = "Python client for Card Settlement Rail"
requires-python = ">=3.10"
dependencies = []
[tool.setuptools.packages.find]
where = ["src"]
