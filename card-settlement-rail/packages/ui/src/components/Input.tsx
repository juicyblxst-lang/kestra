import * as React from 'react';import {clsx} from 'clsx';import {focusRing} from '../a11y/focus.js';
export type InputProps=React.InputHTMLAttributes<HTMLInputElement>&{label?:string;error?:string;hint?:string};
export const Input=React.forwardRef<HTMLInputElement,InputProps>(function Input({label,error,hint,id,className,...props},ref){const inputId=id??React.useId();return <div className="space-y-1.5"><label htmlFor={inputId} className="block text-sm font-medium text-finance-800">{label}</label><input ref={ref} id={inputId} aria-invalid={!!error} aria-describedby={error?`${inputId}-error`:hint?`${inputId}-hint`:undefined} className={clsx('block w-full rounded-finance border bg-white px-3 py-2.5 text-sm text-finance-900 placeholder:text-finance-400',focusRing,error?'border-danger-500 focus-visible:ring-danger-500':'border-finance-200','disabled:cursor-not-allowed disabled:bg-finance-50 disabled:opacity-60',className)} {...props}/>{error?<p id={`${inputId}-error`} role="alert" className="text-sm text-danger-600">{error}</p>:hint?<p id={`${inputId}-hint`} className="text-xs text-finance-500">{hint}</p>:null}</div>});
