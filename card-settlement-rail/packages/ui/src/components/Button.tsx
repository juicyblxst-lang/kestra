import * as React from 'react';import {clsx} from 'clsx';import {focusRing} from '../a11y/focus.js';
export type ButtonProps=React.ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'danger'|'ghost';loading?:boolean};
export function Button({variant='primary',loading=false,disabled,className,children,...props}:ButtonProps){const styles={primary:'bg-brand-600 text-white hover:bg-brand-700 active:bg-brand-800',secondary:'bg-white text-finance-800 border border-finance-200 hover:bg-finance-50 active:bg-finance-100',danger:'bg-danger-600 text-white hover:bg-danger-700 active:bg-danger-700',ghost:'bg-transparent text-finance-700 hover:bg-finance-100 active:bg-finance-200'}[variant];return <button type={props.type??'button'} aria-busy={loading||undefined} disabled={disabled||loading} className={clsx('inline-flex min-h-10 items-center justify-center gap-2 rounded-finance px-4 py-2 text-sm font-semibold transition-colors disabled:cursor-not-allowed disabled:opacity-50',focusRing,styles,className)} {...props}>{loading&&<span aria-hidden="true" className="h-4 w-4 animate-spin rounded-full border-2 border-current border-r-transparent"/>}{loading&&typeof children==='string'?'Loading…':children}</button>}
