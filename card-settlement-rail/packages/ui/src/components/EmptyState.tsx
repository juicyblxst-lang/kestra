import * as React from 'react';export function EmptyState({title='No data',description,action}:{title?:string;description?:string;action?:React.ReactNode}){return <div className="flex min-h-40 flex-col items-center justify-center p-6 text-center"><div className="mb-3 rounded-full bg-finance-100 p-3 text-finance-500" aria-hidden="true">∅</div><h3 className="text-sm font-semibold text-finance-900">{title}</h3>{description&&<p className="mt-1 max-w-sm text-sm text-finance-500">{description}</p>}{action&&<div className="mt-4">{action}</div>}</div>}
