import * as React from 'react';import {clsx} from 'clsx';export type Status='pending'|'processing'|'success'|'failed'|'warning'|'neutral';
export function StatusPill({status,label}:{status:Status;label?:string}){const styles={pending:'bg-warning-50 text-warning-700',processing:'bg-brand-50 text-brand-700',success:'bg-success-50 text-success-700',failed:'bg-danger-50 text-danger-700',warning:'bg-warning-50 text-warning-700',neutral:'bg-finance-100 text-finance-700'};return <span role="status" className={clsx('inline-flex items-center rounded-full px-2.5 py-1 text-xs font-semibold',styles[status])}><span aria-hidden="true" className="mr-1.5 h-1.5 w-1.5 rounded-full bg-current"/>{label??status}</span>}
