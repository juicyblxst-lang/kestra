import * as React from 'react';import * as SelectPrimitive from '@radix-ui/react-select';import {clsx} from 'clsx';import {focusRing} from '../a11y/focus.js';
export type SelectOption={value:string;label:string;disabled?:boolean};
export function Select({label,options,error,placeholder='Select…',value,onValueChange,disabled}:{label?:string;options:SelectOption[];error?:string;placeholder?:string;value?:string;onValueChange?:(v:string)=>void;disabled?:boolean}){const id=React.useId();return <div className="space-y-1.5"><label htmlFor={id} className="block text-sm font-medium text-finance-800">{label}</label><SelectPrimitive.Root value={value} onValueChange={onValueChange} disabled={disabled}><SelectPrimitive.Trigger id={id} aria-invalid={!!error} className={clsx('flex min-h-10 w-full items-center justify-between rounded-finance border bg-white px-3 py-2 text-sm',focusRing,error?'border-danger-500':'border-finance-200')}><SelectPrimitive.Value placeholder={placeholder}/><SelectPrimitive.Icon>⌄</SelectPrimitive.Icon></SelectPrimitive.Trigger><SelectPrimitive.Portal><SelectPrimitive.Content position="popper" className="z-50 overflow-hidden rounded-finance border border-finance-200 bg-white shadow-card"><SelectPrimitive.Viewport className="p-1">{options.map(o=><SelectPrimitive.Item key={o.value} value={o.value} disabled={o.disabled} className="cursor-default rounded px-3 py-2 text-sm outline-none data-[highlighted]:bg-brand-50 data-[highlighted]:text-brand-700 data-[disabled]:opacity-40"><SelectPrimitive.ItemText>{o.label}</SelectPrimitive.ItemText></SelectPrimitive.Item>)}</SelectPrimitive.Viewport></SelectPrimitive.Content></SelectPrimitive.Portal></SelectPrimitive.Root>{error&&<p role="alert" className="text-sm text-danger-600">{error}</p>}</div>}
