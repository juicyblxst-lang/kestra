import * as React from 'react';import * as D from '@radix-ui/react-dialog';import {clsx} from 'clsx';import {focusRing} from '../a11y/focus.js';
export const Dialog=D.Root;export const DialogTrigger=D.Trigger;export const DialogTitle=D.Title;export const DialogDescription=D.Description;export const DialogClose=D.Close;
export function DialogContent({children,className,...props}:React.ComponentPropsWithoutRef<typeof D.Content>){return <D.Portal><D.Overlay className="fixed inset-0 z-40 bg-finance-950/50"/><D.Content {...props} className={clsx('fixed left-1/2 top-1/2 z-50 w-[min(92vw,32rem)] -translate-x-1/2 -translate-y-1/2 rounded-xl border border-finance-200 bg-white p-6 shadow-2xl',className)}>{children}<D.Close aria-label="Close dialog" className={clsx('absolute right-4 top-4 rounded p-1 text-finance-500 hover:bg-finance-100',focusRing)}>×</D.Close></D.Content></D.Portal>}
