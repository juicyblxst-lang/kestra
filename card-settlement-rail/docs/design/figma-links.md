# Figma links

This repository currently has no authoritative Figma file linked. Do not invent a design URL.

When the canonical Figma file is approved, add it here with:

- Product design file
- Component library
- Accessibility annotations
- Settlement operations flows

Until then, `apps/design-preview` is the executable source for component behavior and states.
